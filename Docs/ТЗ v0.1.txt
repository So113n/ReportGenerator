В общем, что по поводу программы, у меня тема диплома "Разработка программного обеспечения, для автоматизации процесса генерации отчетов об компьютерных инцидентах в организации ООО «Уренгойаэроинвест»".

Исходя из темы можно понять, мне нужна программа по автоматической генерации отчетности, научный рук. запретил все ЯП кроме C#.

У меня на удаленном сервере стоит программа GLPI (данные от сервера для RDP подключения я предоставлю). GLPI - это веб приложение написанное на PHP которое представляет из себя систему управления заявками и инцидентами. БД в которой хранятся все данные задействовано с помощью MySQL.

Если вкратце, то мне нужна программа с UI интерфейсом (скорей всего WMF или WinForms) написанная на C# которая будет иметь прямой доступ к MySQL, которая в свою очередь будет отправлять SQL запросы в БД из соответствующих таблиц в (скриншот 1), и забирать от них соответвующие данные и на основе их, будет генировать отчет в формате Excel, вот в таком виде (Скриншот 2). 

По самой программе, как я вижу, я бы хотел чтобы была исполняемым файлом в формате EXE (если возможно), ну и примерный дизайн интерфеса указан на скриншоте 3.